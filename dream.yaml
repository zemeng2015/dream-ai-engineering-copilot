# SPDX-License-Identifier: Apache-2.0

mode: public-demo

llm:
  provider: mock

knowledge:
  pack_root: knowledge_packs

artifacts:
  root: artifacts

audit:
  sqlite_path: dream.sqlite

redaction:
  provider: default

prompt_policy:
  provider: default
