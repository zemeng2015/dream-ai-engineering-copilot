package com.democorp.dfp.adapters;

/** Synthetic DFP code for DREAM codebase memory demos. */
public class StorageAdapter { public String previewSmallObject(String storageKey, int page) { if (storageKey == null) { return "permission-denied"; } return "preview-page-" + page; } }
