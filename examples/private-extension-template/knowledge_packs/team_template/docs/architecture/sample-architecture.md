# Sample Architecture

PrivateDemoApp uses a simple request handler, a deterministic workflow service, and a local artifact boundary.

Generated artifacts should be written only through the configured DREAM artifact root.
